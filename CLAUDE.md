@AGENTS.md

Before running project commands in a new Conductor workspace, follow the `Workspace startup` procedure imported from `AGENTS.md`. In particular, use the Conductor `dev` Run to provision the workspace Supabase branch; do not start with bare `npm run dev`.
